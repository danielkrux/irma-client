import React from 'react';
import { observer } from 'mobx-react';

export interface TeamProps {
  
}
 
const Team: React.SFC<TeamProps> = observer(() => {
  return ( 
    <div style={{minHeight: '100vh'}}>

    </div>
   );
})
 
export default Team;